- name: install the all packages
  hosts: all
  become: yes
  vars_files:
  - credentials.yaml
  tasks:
  - name: install the postfix
      name: postfix
      state: latest

  - name: install the gcc
      name: gcc
      state: latest

  
